# Infrastructure

Infrastructure-specific assets will be added here as the platform evolves.

In V0, the local PostgreSQL service is intentionally defined in the repository-root `docker-compose.yml`, following the roadmap structure.
