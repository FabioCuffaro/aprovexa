# Local PostgreSQL configuration for Docker Compose.
# Copy this file to .env before starting infrastructure.
POSTGRES_DB=aprovexa
POSTGRES_USER=aprovexa
POSTGRES_PASSWORD=change_me_local_only
POSTGRES_PORT=5432
